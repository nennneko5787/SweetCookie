# rp-ui

<!-- GENERATED by ./gradlew specReport --write - do not edit. -->

Specification: [SC-180](../../spec/normative/) &middot; 
Ledger: [`spec/coverage/rp-ui.yaml`](../../spec/coverage/rp-ui.yaml)

| Feature | Status | Notes |
|---|---|---|
| `json_ui/screens` | wontfix | JSON UI files are recognised and ignored, so a pack that replaces or adds a screen shows the vanilla Java screen instead and any custom layout, artwork or controls are absent. |
| `json_ui/modifications` | wontfix | Patching vanilla Bedrock screens has no Java target, so modifications are ignored entirely and the vanilla Java screen is unchanged. |
| `json_ui/bindings` | wontfix | The engine-provided binding namespace is undocumented and Bedrock-specific, so no bound value is available and nothing that depends on one renders. |
