# rp-materials

<!-- GENERATED by ./gradlew specReport --write - do not edit. -->

Specification: [SC-180](../../spec/normative/) &middot; 
Ledger: [`spec/coverage/rp-materials.yaml`](../../spec/coverage/rp-materials.yaml)

| Feature | Status | Notes |
|---|---|---|
| `material/inheritance` | stub |  |
| `material/states` | stub | Blending, DisableCulling, DisableDepthWrite, EnableAlphaToCoverage, StencilWrite. |
| `material/depth_func` | stub |  |
| `material/variants` | stub |  |
| `material/defines` | stub |  |
| `material/vertex_shader` | wontfix | Bedrock names shader programs that do not exist on Java; a pack shipping custom shaders renders with the nearest standard Java render type instead, so bespoke visual effects are absent. |
| `material/fragment_shader` | wontfix | Bedrock names shader programs that do not exist on Java; a pack shipping custom shaders renders with the nearest standard Java render type instead, so bespoke visual effects are absent. |
| `material/stencil_ops` | wontfix | Stencil configuration has no exposed Java equivalent, so effects relying on stencil masking render without the mask and may show geometry that Bedrock would have hidden. |
