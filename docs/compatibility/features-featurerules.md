# features-featurerules

<!-- GENERATED by ./gradlew specReport --write - do not edit. -->

Specification: [SC-190](../../spec/normative/) &middot; 
Ledger: [`spec/coverage/features-featurerules.yaml`](../../spec/coverage/features-featurerules.yaml)

| Feature | Status | Notes |
|---|---|---|
| `minecraft:aggregate_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:sequence_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:ore_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:scatter_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:single_block_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:structure_template_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `minecraft:tree_feature` | unsupported | Worldgen features are parsed and reported but never placed, so a pack that adds custom ore, trees or structures generates a world identical to one without it. |
| `feature_rules/distribution` | unsupported | Feature rule scatter parameters have no Java placement-modifier equivalent, so no custom feature is ever scheduled for generation regardless of its rule. |
